#include <conio.h>
#include <stdio.h>
int main()

{
	char password[10].username[10].ch:
	int i;
	clrscr[];
	printf("Enter username:");
	gets(username);
	printf("Enter 8 digit password::");
	for(i = 0; 1 < 8: i++)
	{
	char = getchar();
	password[i] = ch;
	ch = '*';
	print("%c"; ch);
	}
	password[i] = '';
	printf(i = 0, i < 8; i++)
	{
	printf("%c", password[i]);
	}
	getchar();
	return 0;
}
